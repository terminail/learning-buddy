#include <iostream>
#include <set>
#include <utility>
#include <string>

// TODO: Work with std::pair and multiset containers
// Example approach:
// 1. Create and use std::pair objects
// 2. Create a multiset of pairs to store duplicate pairs
// 3. Insert pairs into the multiset
// 4. Access and iterate through pairs in the multiset

int main() {
    // TODO: Create and use std::pair objects
    // - Create pairs using different methods (constructor, make_pair, {})
    // - Access first and second elements of pairs
    
    // TODO: Create a multiset of pairs to store duplicate pairs
    // - Declare a multiset that stores pairs (e.g., pair<string, int>)
    
    // TODO: Insert pairs into the multiset
    // - Insert multiple identical pairs
    // - Use different insertion methods
    
    // TODO: Access and iterate through pairs in the multiset
    // - Count occurrences of specific pairs
    // - Find pairs in the multiset
    // - Iterate through all pairs
    
    return 0;
}