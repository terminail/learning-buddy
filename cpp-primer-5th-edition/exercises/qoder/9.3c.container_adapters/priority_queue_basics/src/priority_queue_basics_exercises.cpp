// Exercise 9.3c: Priority Queue Basics
// This exercise introduces you to the std::priority_queue container adapter in C++.
// Your task is to work with priority queue operations like push and pop with priority ordering.

#include <iostream>
#include <queue>
#include <vector>

int main() {
    // TODO: Create an empty priority queue of integers (max-heap by default)
    // Example approach:
    // 1. Declare a priority_queue of integers using std::priority_queue<int>
    // 2. The priority queue should be empty initially
    
    // TODO: Push several elements into the priority queue
    // Example approach:
    // 1. Use push() method to add elements to the priority queue
    // 2. Add at least 5 different integers to the priority queue
    // 3. Note that elements will be ordered by priority (largest first by default)
    
    // TODO: Access the top element of the priority queue without removing it
    // Example approach:
    // 1. Use top() method to access the highest priority element
    // 2. Print the value of the top element
    
    // TODO: Remove elements from the priority queue
    // Example approach:
    // 1. Use pop() method to remove the top element
    // 2. Remove several elements from the priority queue
    // 3. Observe the order in which elements are removed
    
    // TODO: Check if the priority queue is empty
    // Example approach:
    // 1. Use empty() method to check if priority queue has no elements
    // 2. Print whether the priority queue is empty or not
    
    // TODO: Get the size of the priority queue
    // Example approach:
    // 1. Use size() method to get the number of elements
    // 2. Print the size of the priority queue
    
    return 0;
}