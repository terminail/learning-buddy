// Exercise 5.1.1: Control Flow Statements
// This exercise introduces you to C++ control flow statements: if/else and loops.
// Your task is to create programs that make decisions and repeat actions.

#include <iostream>
#include <string>

int main() {
    // TODO: Create a program that determines if a number is positive, negative, or zero
    // Example approach:
    // 1. Declare an integer variable to store the user's input
    // 2. Prompt the user to enter a number
    // 3. Read the number using std::cin
    // 4. Use if/else if/else statements to check:
    //    - If the number is greater than 0 (positive)
    //    - If the number is less than 0 (negative)
    //    - Otherwise it must be 0
    // 5. Print the appropriate message for each case
    
    // TODO: Create a program that prints numbers from 1 to 10 using a for loop
    // Example approach:
    // 1. Use a for loop that starts at 1 and continues while the counter is <= 10
    // 2. Inside the loop, print the current value of the counter
    // 3. Add a space between numbers for readability
    // 4. After the loop, print a newline to move to the next line
    
    // TODO: Create a program that calculates the sum of numbers from 1 to n using a while loop
    // Example approach:
    // 1. Declare variables for n (user input), sum (initialized to 0), and i (counter, initialized to 1)
    // 2. Prompt the user to enter a number and read it into n
    // 3. Use a while loop that continues while i <= n
    // 4. Inside the loop:
    //    - Add i to sum
    //    - Increment i
    // 5. After the loop, print the final sum value
    
    // TODO: Create a program that prints a pattern of stars using nested loops
    // Example approach:
    // 1. Declare a variable to store the number of rows
    // 2. Prompt the user to enter the number of rows
    // 3. Read the user input into your variable
    // 4. Use a nested loop structure:
    //    - Outer loop iterates from 1 to the number of rows
    //    - Inner loop iterates from 1 to the current row number
    //    - Print a star '*' in the inner loop
    //    - Print a newline after each row in the outer loop
    
    return 0;
}