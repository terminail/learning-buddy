#include <iostream>
#include <map>
#include <string>
#include <vector>

// TODO: Work with std::pair and multimap containers
// Example approach:
// 1. Create and use std::pair objects
// 2. Create a multimap to store multiple values for the same key
// 3. Insert elements into the multimap
// 4. Access and iterate through elements in the multimap

int main() {
    // TODO: Create and use std::pair objects
    // - Create pairs using different methods (constructor, make_pair, {})
    // - Access first and second elements of pairs
    
    // TODO: Create a multimap to store multiple values for the same key
    // - Declare a multimap (e.g., string -> int)
    
    // TODO: Insert elements into the multimap
    // - Insert multiple values for the same key
    // - Use different insertion methods
    
    // TODO: Access and iterate through elements in the multimap
    // - Find all values for a specific key
    // - Iterate through all elements
    
    return 0;
}